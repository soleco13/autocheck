-- Список тем, пройденных в рамках материала (по всем заданиям, не только
-- с ошибками) — генерируется ИИ вместе с остальной сводкой отчёта и
-- выводится в docx-отчёте для ученика.
ALTER TABLE reports ADD COLUMN IF NOT EXISTS ai_topics_covered TEXT;
